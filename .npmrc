# Cooldown supply-chain (QM SECURITY.md §Dependency cooldown): versiones con
# <7 días no entran al lockfile. Requiere npm >= 11.10 (ver engines en package.json).
# Bypass puntual de hotfix: instalar la versión exacta explícitamente.
min-release-age=7
